---
title: Overview
description: Canonical metadata for the OpenMusic ecosystem.
icon: pixelarticons:text-search
---

<Callout title="Attention" type="error">
  This is still in development. It is not final yet, and breaking changes can
  happen at any time.
</Callout>

OpenMusicMetadata is a read-only metadata API for canonical songs, albums, and artists. OpenMusic servers can use OpenMusicMetadata ids to map the same musical entity across different catalogs.

## Resource Types

- `song`
- `album`
- `artist`

## Main Workflows

Use `/lookup/{id}` or `/lookup?ids=...` when you already have OpenMusicMetadata ids.

```http
GET /lookup/omm:song:1
GET /lookup?ids=omm:song:1,omm:album:2
```

Use `/search/{type}` when you need to find canonical metadata by name, album name, or artist name.

```http
GET /search/song?name=blue&artist=example
```
